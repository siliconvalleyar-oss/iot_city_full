name: iot_city_flt
description: "IoT City — Modern Urban Dashboard for Smart City Metrics"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^0.70.2
  provider: ^6.1.2
  google_fonts: ^6.2.1
  intl: ^0.20.2
  web_socket_channel: ^3.0.2
  http: ^1.3.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
